export type TBook = {
    id: string;
    author: string;
    cover_url: string;
    description: string;
    title: string;
  };

  export type TUser = {
    username: string;
    password: string;
  }